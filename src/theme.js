import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            light: '#656565',
            dark: '#181818',
            main: '#111111',
        }
    }
})

export default theme;