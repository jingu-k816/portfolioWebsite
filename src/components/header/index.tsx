import { Container } from "@mui/material";

const Header: React.FC = () => {
    return (
        <Container sx={{ maxWidth: "100%", backgroundColor: "#181818", maxHeight: "10vh" }}>

        </Container>
    );
}
export default Header;