import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            light: '#181818',
            dark: '#000000',
            main: '#111111',
        },
        whites: {
            main: "#ffffff",
            dark: "#e6e6e6"
        }
    }
})

export default theme;